<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="io.choerodon.devops.infra.mapper.DevopsGitlabPersonalTokensMapper">

    <select id="queryByGitlabUserId" resultType="io.choerodon.devops.infra.dataobject.DevopsGitlabPersonalTokensDO">
        select
          dgpt.id
          dgpt.gitlab_user_id
          dgpt.user_id
          dgpt.token
        from devops_gitlab_personal_tokens dgpt
        where dgpt.gitlab_user_id = #{gitlabUserId}
    </select>
</mapper>
